# Architecture — AI MIDI Composer

## Documentos

- `overview.md` - visão de modulos e dependências (a preencher).
- `modules/<domain>-<module>.md` - 1 por modulo musical/engine (criados quando
  modulos forem adicionados).
- Visão alinhada a `standards/architecture.md` e `prompts/master_prompt.md` §3.

## Diagramas

Referenciar em `docs/diagrams/` (PlantUML). Ver skills `doc-standards` para rendered
process no CI.
